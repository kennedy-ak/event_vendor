# Core Django
Django>=5.0,<6.0
python-dotenv==1.0.0

# Database
psycopg2-binary>=2.9.9  # PostgreSQL adapter
# django-postgis==0.0.1  # PostGIS support - Removed as it's causing issues and not strictly required for basic geo

# Image handling
Pillow==10.2.0  # Image processing
django-storages==1.14.2  # Cloud storage backends (S3, DigitalOcean Spaces, etc.)
boto3==1.34.28  # AWS S3 SDK

# Forms and UI
django-crispy-forms==2.1
crispy-bootstrap5>=2.0

# API and CORS
django-cors-headers>=4.3.1

# Payment Gateway
pypaystack2>=1.1.0  # Paystack Python SDK

# AI Chatbot
openai>=1.10.0  # OpenAI API for chatbot

# Geospatial
# django-geopy==0.6  # Geocoding - Removed as it's causing issues
geopy==2.4.1  # Location utilities

# Production server
gunicorn==21.2.0  # WSGI server
whitenoise==6.6.0  # Static file serving
